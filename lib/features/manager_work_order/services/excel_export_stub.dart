/// Stub implementation — never actually called at runtime.
/// The correct platform import replaces this via conditional import.
Future<void> saveExcelFile(List<int> bytes, String filename) {
  throw UnsupportedError('Platform not supported');
}
